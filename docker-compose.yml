version: '3.8'

services:
  web:
    build: .
    container_name: ipl_fantasy_web
    ports:
      - "8000:8000"
    environment:
      # Required settings
      - SECRET_KEY=${SECRET_KEY:-change_me_in_production}
      - RAPIDAPI_KEY=${RAPIDAPI_KEY}
      
      # Environment configuration
      - ENVIRONMENT=${ENVIRONMENT:-production}
      - HOST=0.0.0.0
      - PORT=8000
      - RELOAD=false
      
      # Logging
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FILE=logs/app.log
      
      # Security
      - RATE_LIMIT=${RATE_LIMIT:-60}
      - CORS_ORIGINS=${CORS_ORIGINS:-*}
      - FORCE_HTTPS=${FORCE_HTTPS:-false}
    volumes:
      # Persist data between container restarts
      - ./data:/app/data
      - ./logs:/app/logs
      - ./Match data:/app/Match data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

# ══════════════════════════════════════════════════════════════
# NGINX Reverse Proxy (Optional - for SSL/domain setup)
# Uncomment when deploying to a domain with SSL
# ══════════════════════════════════════════════════════════════

#  nginx:
#    image: nginx:alpine
#    container_name: ipl_fantasy_nginx
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./deployment/nginx.conf:/etc/nginx/conf.d/default.conf
#      - ./web/static:/usr/share/nginx/html/static
#      - ./certbot/conf:/etc/letsencrypt
#      - ./certbot/www:/var/www/certbot
#    depends_on:
#      - web
#    restart: unless-stopped

# ══════════════════════════════════════════════════════════════
# Certbot for SSL Certificates (Optional)
# Uncomment when setting up Let's Encrypt SSL
# ══════════════════════════════════════════════════════════════

#  certbot:
#    image: certbot/certbot
#    container_name: ipl_fantasy_certbot
#    volumes:
#      - ./certbot/conf:/etc/letsencrypt
#      - ./certbot/www:/var/www/certbot
#    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"

# ══════════════════════════════════════════════════════════════
# Redis for Rate Limiting (Optional)
# Uncomment if you want distributed rate limiting
# ══════════════════════════════════════════════════════════════

#  redis:
#    image: redis:7-alpine
#    container_name: ipl_fantasy_redis
#    ports:
#      - "6379:6379"
#    volumes:
#      - redis_data:/data
#    restart: unless-stopped

# volumes:
#   redis_data:
